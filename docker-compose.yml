version: "3.9"
services:
  telogs:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - TELOGS_SECRET=${TELOGS_SECRET:-dev-secret}
    working_dir: /app
    volumes:
      - ./:/app
    command: ["bash", "-lc", "make demo && tail -f /dev/null"]